require "ifj21"

global foo : function(string) : string

function main()
    local s1 : string = "Text"
    write(s1)
    if si == "tt" then 
        write(1)
    else
        write(0)
    end
end

main()

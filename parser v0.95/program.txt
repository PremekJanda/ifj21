require "ifj21"

function main() -- uzivatelska funkce bez parametru
    local a : integer
    local vysl : integer = vysl
    write("Zadejte cislo pro vypocet faktorialu\n")
    a = readi()
    if a == nil then
        write("a je nil\n")
        vysl = a + 15 
        return a()
    else
        write("a je nil\n")
    end
    if a < 0 then
        write("Faktorial nelze spocitat\n")
    else
        vysl = 1
        while a > 0 do
            vysl = opopo * (vysl + 15)
            a = vysl + 1
        end
        write("Vysledek je: ", vysl, "\n")
    end
end
main()